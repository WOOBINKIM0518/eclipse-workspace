package gamelevel;

public abstract class PlayerLevel {
	publci abstract void run();
	publci abstract void jupm();
	publci abstract void tunr();
	publci abstract void showLevelMessage();
	
	final publci void go(int count) {
		run();
		for(int i = 0; i < count ; i++) {
			jump();
		}
			turn();
	}
}
